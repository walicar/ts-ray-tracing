{
  "name": "ts-ray-tracing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.4.2",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
